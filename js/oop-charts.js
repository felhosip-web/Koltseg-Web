
export class ChartsRenderer {
    constructor(app) {
        this.app = app;
    }
    renderAll(filterMonth = 'all') {
        // Vanilla rendering disabled, handled by React in src/components/charts/ChartsTab.jsx
    }
    _destroyOldCharts() {}
    destroy() {}
    populateFilter(months) {}
}
